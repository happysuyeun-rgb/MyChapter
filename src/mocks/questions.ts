import { DEV_MOCK_TODAY_QUESTION } from '@/mocks/data'

export function mockGenerateQuestion(): string {
  return DEV_MOCK_TODAY_QUESTION
}

export function mockGetTodayQuestion(): string | null {
  return DEV_MOCK_TODAY_QUESTION
}
